/**
 * MoarTube-Node TypeScript Entry Point
 *
 * This is a placeholder entry point for Phase 0.
 * The actual application logic will be migrated in subsequent phases.
 */

// Placeholder - will be replaced with Pino logger in Phase 1
const log = (message: string): void => {
  // eslint-disable-next-line no-console
  console.log(message);
};

// Verify TypeScript compilation works
log('MoarTube-Node TypeScript build successful');

export {};
